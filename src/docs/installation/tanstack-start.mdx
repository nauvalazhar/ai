---
parts: []
---

Install the kit in a TanStack Start project. The same flow works for standalone TanStack Router.

## Create project

Scaffold a TanStack Start app. The CLI configures Tailwind and the `@/*` alias for you.

<CodeTabs>
```bash title="bun"
bunx --bun @tanstack/cli@latest create
```

```bash title="npm"
npx @tanstack/cli@latest create
```

```bash title="pnpm"
pnpm dlx @tanstack/cli@latest create
```

```bash title="yarn"
yarn dlx @tanstack/cli@latest create
```

</CodeTabs>

For a router-only SPA (no SSR), pass `--router-only`.

## Initialize

Run init to install base deps, append the token CSS, and create the `cn` helper.

<CodeTabs>
```bash title="bun"
bunx @nauvalazhar/ai@latest init
```

```bash title="npm"
npx @nauvalazhar/ai@latest init
```

```bash title="pnpm"
pnpm dlx @nauvalazhar/ai@latest init
```

```bash title="yarn"
yarn dlx @nauvalazhar/ai@latest init
```

</CodeTabs>

The CLI prompts to confirm before touching any files. Pass `-y` to skip.

## Add a component

<CodeTabs>
```bash title="bun"
bunx @nauvalazhar/ai@latest add message
```

```bash title="npm"
npx @nauvalazhar/ai@latest add message
```

```bash title="pnpm"
pnpm dlx @nauvalazhar/ai@latest add message
```

```bash title="yarn"
yarn dlx @nauvalazhar/ai@latest add message
```

</CodeTabs>

The file lands at `src/components/ai/message.tsx` with imports rewritten to your alias. Use it from any route:

```tsx title="src/routes/index.tsx"
import { createFileRoute } from "@tanstack/react-router";
import { Message, MessageContent, MessageText } from "@/components/ai/message";

export const Route = createFileRoute("/")({ component: Index });

function Index() {
  return (
    <Message>
      <MessageContent>
        <MessageText variant="bubble">Hi, there!</MessageText>
      </MessageContent>
    </Message>
  );
}
```

## Dark mode

Set `.dark` on `<html>` in `src/routes/__root.tsx`, either statically or from a theme store:

```tsx title="src/routes/__root.tsx"
export const Route = createRootRoute({
  component: () => (
    <html lang="en" className="dark">
      <head>{/* ... */}</head>
      <body>
        <Outlet />
      </body>
    </html>
  ),
});
```

The kit reads `.dark` from any ancestor, so a theme store paired with a `useEffect` toggling the class also works.

## Font family (recommended)

The kit was designed around Inter and JetBrains Mono. Add the following to `src/styles.css`:

```css title="src/styles.css"
@theme inline {
  --font-sans:
    "Inter", ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji",
    "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
  --font-mono:
    "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
    "Liberation Mono", "Courier New", monospace;
}
```

## Icons (optional)

Components ship without a bundled icon library. Use any set you like. The demos on this site use `lucide-react`.

## Skipping the CLI

Prefer to copy files by hand? See [Manual setup](/installation/manual).
